name: biometrics
description: Biometrics module
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  local_auth: ^2.3.0
  get_it: ^7.7.0

  core:
    path: ../../core

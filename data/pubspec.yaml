name: data
version: 0.0.1
publish_to: none
description: Project data module

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  domain:
    path: ../domain
  supabase_flutter: ^2.8.0
  firebase_core: ^3.7.0
  firebase_auth: ^5.3.2
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.8
